% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildCooccurrenceNetwork.R
\name{buildCooccurrenceNetwork}
\alias{buildCooccurrenceNetwork}
\title{Build a Co-occurrence Network from Microbiome Data}
\usage{
buildCooccurrenceNetwork(
  data,
  method = "spearman",
  cor_threshold = 0.6,
  pval_threshold = 0.05,
  min_prevalence = 0.1
)
}
\arguments{
\item{data}{A numeric matrix (rows = samples, cols = taxa).}

\item{method}{Character string: \code{"spearman"} or \code{"pearson"}.
Default: \code{"spearman"}.}

\item{cor_threshold}{Numeric. Minimum absolute correlation for an edge.
Default: \code{0.6}.}

\item{pval_threshold}{Numeric. Maximum p-value for an edge.
Default: \code{0.05}.}

\item{min_prevalence}{Numeric in [0, 1]. Prevalence filter passed to
\code{\link{calculateCorrelation}}. Default: \code{0.1}.}
}
\value{
A list with:
  \describe{
    \item{graph}{An \code{igraph} graph object.}
    \item{node_metrics}{data.frame with degree, betweenness, community.}
    \item{edge_list}{data.frame of edges with correlation and p-value.}
    \item{n_nodes}{Integer.}
    \item{n_edges}{Integer.}
    \item{modularity}{Numeric modularity score.}
  }
}
\description{
Constructs an \code{igraph} co-occurrence network from pairwise taxon
correlations, filtering edges by correlation strength and significance,
and performing community detection.
}
\examples{
\donttest{
set.seed(42)
counts <- matrix(rpois(200, 15), nrow = 20, ncol = 10)
rownames(counts) <- paste0("S", 1:20)
colnames(counts) <- paste0("OTU", 1:10)
net <- buildCooccurrenceNetwork(counts, cor_threshold = 0.3)
net$n_edges
}

}
