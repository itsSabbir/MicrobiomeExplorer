% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotVolcano.R
\name{plotVolcano}
\alias{plotVolcano}
\title{Volcano Plot for Differential Abundance Results}
\usage{
plotVolcano(
  de_results,
  fc_col = "log2FoldChange",
  pval_col = "pvalue",
  fc_threshold = 1,
  pval_threshold = 0.05,
  label_col = NULL,
  point_size = 2,
  colors = c(Up = "#d73027", Down = "#4575b4", NS = "grey60"),
  pval_floor = 1e-300
)
}
\arguments{
\item{de_results}{A data.frame with differential expression results.}

\item{fc_col}{Character string naming the fold-change column.
Default: \code{"log2FoldChange"} (DESeq2 output). For edgeR use
\code{"logFC"}.}

\item{pval_col}{Character string naming the p-value column.
Default: \code{"pvalue"} (DESeq2). For edgeR use \code{"PValue"}.}

\item{fc_threshold}{Numeric. Absolute fold-change threshold for colour
coding. Default: \code{1}.}

\item{pval_threshold}{Numeric. P-value threshold for colour coding.
Default: \code{0.05}.}

\item{label_col}{Optional character string naming a column with point
labels. If \code{NULL}, row names are used. Default: \code{NULL}.}

\item{point_size}{Numeric. Size of plotted points. Default: \code{2}.}

\item{colors}{Named character vector of three colours for "Up", "Down", and
"NS" significance categories. Default: \code{c(Up = "#d73027", Down =
"#4575b4", NS = "grey60")}.}

\item{pval_floor}{Numeric. Minimum p-value floor to avoid \code{log(0)}.
Default: \code{1e-300}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a volcano plot (log2 fold-change vs. -log10 p-value) from
differential abundance analysis results.
}
\examples{
set.seed(42)
res <- data.frame(
  log2FoldChange = rnorm(50),
  pvalue = runif(50),
  taxon = paste0("OTU", 1:50)
)
p <- plotVolcano(res, label_col = "taxon")
print(p)

}
