% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performClustering.R
\name{performClustering}
\alias{performClustering}
\title{Unsupervised Clustering of Microbiome Samples}
\usage{
performClustering(
  data,
  method = "kmeans",
  k = NULL,
  max_k = 10,
  dist_method = "bray",
  dbscan_eps = NULL,
  dbscan_minPts = 5,
  scale_data = TRUE
)
}
\arguments{
\item{data}{A numeric matrix (rows = samples, cols = taxa).}

\item{method}{Character string: \code{"kmeans"}, \code{"hierarchical"}, or
\code{"dbscan"}. Default: \code{"kmeans"}.}

\item{k}{Integer or \code{NULL}. Number of clusters; \code{NULL} triggers
automatic selection. Default: \code{NULL}.}

\item{max_k}{Integer. Maximum k to evaluate during auto-selection.
Default: \code{10}.}

\item{dist_method}{Character string for distance calculation (passed to
\code{\link{calculateBetaDiversity}}). Default: \code{"bray"}.}

\item{dbscan_eps}{Numeric. DBSCAN epsilon. Default: \code{NULL} (auto).}

\item{dbscan_minPts}{Integer. DBSCAN minimum points. Default: \code{5}.}

\item{scale_data}{Logical. Centre and scale before clustering.
Default: \code{TRUE}.}
}
\value{
A list with:
  \describe{
    \item{cluster_assignments}{Named integer vector of cluster labels.}
    \item{method}{Character string.}
    \item{k}{Final number of clusters.}
    \item{avg_silhouette}{Mean silhouette width.}
    \item{silhouette_scores}{Per-sample silhouette widths (when
      \code{cluster} package is available).}
  }
}
\description{
Clusters samples using k-means, hierarchical, or DBSCAN methods. When
\code{k = NULL}, the optimal cluster count is selected via silhouette width.
}
\examples{
\dontrun{
counts <- matrix(rpois(100, 15), nrow = 20, ncol = 5)
rownames(counts) <- paste0("S", 1:20)
cl <- performClustering(counts, method = "kmeans", k = 3)
table(cl$cluster_assignments)
}

}
