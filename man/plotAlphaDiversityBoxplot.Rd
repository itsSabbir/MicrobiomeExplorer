% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotAlphaDiversityBoxplot.R
\name{plotAlphaDiversityBoxplot}
\alias{plotAlphaDiversityBoxplot}
\title{Boxplot or Violin Plot of Alpha Diversity by Group}
\usage{
plotAlphaDiversityBoxplot(
  alpha_results,
  sample_info,
  group_var,
  index = "Shannon",
  plot_type = "box",
  add_points = TRUE,
  test = "kruskal"
)
}
\arguments{
\item{alpha_results}{A data.frame from \code{calculateAlphaDiversity()}.
Must have a \code{Sample} column and one column per diversity index.}

\item{sample_info}{A data.frame with sample metadata. Row names or a column
named \code{"Sample"} must match \code{alpha_results$Sample}.}

\item{group_var}{Character string naming the grouping column in
\code{sample_info}. Required.}

\item{index}{Character string naming the diversity index column to plot.
Default: \code{"Shannon"}.}

\item{plot_type}{Character string: \code{"box"} or \code{"violin"}.
Default: \code{"box"}.}

\item{add_points}{Logical. If \code{TRUE}, individual sample points are
overlaid using \code{geom_jitter}. Default: \code{TRUE}.}

\item{test}{Character string: \code{"kruskal"} (Kruskal-Wallis) or
\code{"wilcoxon"} (pairwise Wilcoxon). Default: \code{"kruskal"}.}
}
\value{
A list with:
  \describe{
    \item{plot}{A \code{ggplot} object.}
    \item{test_result}{Result object from the chosen statistical test.}
  }
}
\description{
Visualises alpha diversity distributions across sample groups using box or
violin plots with optional statistical testing.
}
\examples{
set.seed(42)
counts <- matrix(rpois(50, lambda = 20), nrow = 10, ncol = 5)
rownames(counts) <- paste0("S", 1:10)
colnames(counts) <- paste0("OTU", 1:5)
alpha <- calculateAlphaDiversity(counts, indices = c("Shannon", "Simpson"))
meta  <- data.frame(Group = rep(c("A", "B"), 5),
                    row.names = paste0("S", 1:10))
result <- plotAlphaDiversityBoxplot(alpha, sample_info = meta,
                                    group_var = "Group", index = "Shannon")
print(result$plot)

}
