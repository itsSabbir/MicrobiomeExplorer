% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performDimReduction.R
\name{performDimReduction}
\alias{performDimReduction}
\title{Dimensionality Reduction via t-SNE or UMAP}
\usage{
performDimReduction(
  data,
  method = "tSNE",
  perplexity = 30,
  n_neighbors = 15,
  min_dist = 0.1,
  dist_method = "bray",
  scale_data = TRUE,
  seed = 42
)
}
\arguments{
\item{data}{A matrix or data.frame (rows = samples, cols = taxa).}

\item{method}{Character string: \code{"tSNE"} or \code{"UMAP"}.
Default: \code{"tSNE"}.}

\item{perplexity}{Numeric. t-SNE perplexity; auto-capped at
\code{floor((nrow(data) - 1) / 3)} for small datasets. Default: \code{30}.}

\item{n_neighbors}{Integer. Number of UMAP neighbours. Default: \code{15}.}

\item{min_dist}{Numeric. UMAP minimum distance. Default: \code{0.1}.}

\item{dist_method}{Character string. Distance metric for the input distance
matrix (passed to \code{\link{calculateBetaDiversity}}).
Default: \code{"bray"}.}

\item{scale_data}{Logical. Centre and scale columns before computing.
Default: \code{TRUE}.}

\item{seed}{Integer. Random seed for reproducibility. Default: \code{42}.}
}
\value{
A list compatible with \code{\link{plotOrdinationBiplot}}:
  \describe{
    \item{coordinates}{data.frame with \code{Axis1}, \code{Axis2}.}
    \item{method}{Character string (\code{"tSNE"} or \code{"UMAP"}).}
    \item{variance_explained}{\code{NULL} (not applicable).}
    \item{stress}{\code{NULL}.}
  }
}
\description{
Computes 2D embeddings using t-SNE or UMAP, returning coordinates in the
same format as \code{\link{performOrdination}} so that
\code{\link{plotOrdinationBiplot}} works directly.
}
\examples{
\dontrun{
set.seed(42)
counts <- matrix(rpois(100, 15), nrow = 20, ncol = 5)
rownames(counts) <- paste0("S", 1:20)
res <- performDimReduction(counts, method = "tSNE", perplexity = 5)
plotOrdinationBiplot(res)
}

}
