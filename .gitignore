.Rproj.user
.Rhistory
.RData
.Ruserdata
inst/doc

# >>> claude-code project-memory >>>
# Per-machine session memory and notes. Do not commit.
.claude/
# <<< claude-code project-memory <<<

# >>> codex-claude bridge >>>
.codex-runs/
tasks/.*.log
*.bak-*
AGENTS.md.pre-bridge-bak-*
tasks/handoff-log.jsonl
.env
.env.local
.env.*.local
**/.claude/worktrees/
# Public repository: the task ledger stays machine-local, so hide it here too,
# not only in one machine's excludes (which do not travel).
**/tasks/
# <<< codex-claude bridge <<<
.DS_Store
